{
  "name": "todolist",
  "version": "1.0.0",
  "scripts": {
    "start": "http-server -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.0.0"
  }
}
